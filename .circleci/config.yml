version: 2

defaults: &defaults
  working_directory: /go/src/github.com/sbstjn/github-releases-slack
  docker:
    - image: circleci/golang:latest

releases: &releases
  filters:
    tags:
      only: /v[0-9]+(\.[0-9]+)*/
    branches:
      ignore: /.*/

jobs:
  checkout:
    <<: *defaults

    steps:
      - checkout

      - run:
          name: Create .env file
          command: touch .env

      - run:
          name: Install Dependencies
          command: make dependencies

      - persist_to_workspace:
          root: /go/src/github.com/sbstjn/github-releases-slack
          paths:
            - '*'
            - '*.*'

      - save_cache:
          key: v1-pkg-cache
          paths:
            - "/go/pkg"

  test:
    <<: *defaults

    steps:
      - restore_cache:
          keys:
            - v1-pkg-cache

      - attach_workspace:
          at: /go/src/github.com/sbstjn/github-releases-slack

      - run:
          name: Run tests
          command: make test

  build:
    <<: *defaults

    steps:
      - restore_cache:
          keys:
            - v1-pkg-cache

      - attach_workspace:
          at: /go/src/github.com/sbstjn/github-releases-slack

      - run:
          name: Run tests
          command: make build

      - persist_to_workspace:
          root: /go/src/github.com/sbstjn/github-releases-slack
          paths:
            - dist

workflows:
  version: 2

  build:
    jobs:
      - checkout
      - test:
          requires:
            - checkout
      - build:
          requires:
            - test
